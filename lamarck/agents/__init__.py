"""lamarck.agents — decision policies (Phase 0: the deterministic stub).

Public API: :class:`StubPolicy`, the seeded weighted-choice policy behind the
Phase-0 stub world. Real (model-backed) minds arrive in later phases.
"""

from lamarck.agents.stub import StubPolicy

__all__ = ["StubPolicy"]
